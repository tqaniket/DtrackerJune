import { NxtActionPipe } from './nxt-action.pipe';

describe('NxtActionPipe', () => {
  it('create an instance', () => {
    const pipe = new NxtActionPipe();
    expect(pipe).toBeTruthy();
  });
});
