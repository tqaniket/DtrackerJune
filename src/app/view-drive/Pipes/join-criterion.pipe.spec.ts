import { JoinCriterionPipe } from './join-criterion.pipe';

describe('JoinCriterionPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinCriterionPipe();
    expect(pipe).toBeTruthy();
  });
});
