import { EducationcriterionPipe } from './educationcriterion.pipe';

describe('EducationcriterionPipe', () => {
  it('create an instance', () => {
    const pipe = new EducationcriterionPipe();
    expect(pipe).toBeTruthy();
  });
});
