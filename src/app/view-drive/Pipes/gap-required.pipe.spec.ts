import { GapRequiredPipe } from './gap-required.pipe';

describe('GapRequiredPipe', () => {
  it('create an instance', () => {
    const pipe = new GapRequiredPipe();
    expect(pipe).toBeTruthy();
  });
});
